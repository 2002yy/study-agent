# assets/

本目录只保留**当前文档与项目展示需要的静态资源**。

## 当前内容

- `screenshots/`：项目界面截图与架构展示图，用于 README、文档或项目展示。

## 已移除的历史资源

旧 Streamlit 原型时期的角色头像、备用头像、背景图、横幅和 `IMAGE_TODO.md` 已从当前工作树移除。当前 React + FastAPI 运行时不依赖这些文件；如需追溯，可从 Git 历史恢复。

原则：运行时真正依赖的资源才进入仓库；纯候选素材、未接入素材和重复副本不长期保留。
