# Memory Index

> Fast-entry memory file for runtime loading.
> In `fast` mode the app may load this file together with `current_focus.md`.

## Current focus

- v0.7.4 has been released: service extraction, batched flush, CI, architecture tests, llm_client expansion, YAML runtime state
- Current change group: post-v0.7.4 stabilization, wechat module split
- Next functional focus: search quality, source UI, search interaction polish

## Recent milestones

1. v0.7.4 release: wechat service split, batched session flush, CI, architecture tests, llm_client expansion, YAML state
2. v0.7.2 quality pass: bug fixes, caching, cleanup, and stability
3. Current validation baseline: full `pytest` and `ruff check .` passing locally

## Version status

- current_version: v0.7.4
- next_version: v0.7.5
- release_note: `changelog/README_v0_7_3.md`
