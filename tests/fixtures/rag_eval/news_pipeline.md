# News Pipeline Notes

The news pipeline resolves feed entries, normalizes URLs and extracts article text.
Unsafe redirects and private network targets are blocked before article fetching.
Digest generation keeps a source trace so discussion can cite retrieved evidence.
