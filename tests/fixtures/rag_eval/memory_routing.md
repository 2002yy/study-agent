# Memory Routing Notes

Study Agent uses context tiers to control how much memory is loaded.
Fast mode reads a compact summary, while deep mode can include project context.
Model routing chooses role, mode and token budget before the LLM call.
